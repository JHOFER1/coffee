AS an asynchronous event-driven JavaScript runtime, Node.js is designed to buils scalable network applications. In the following 
"hello world" example , many connections can be handled concurrently. Upo  each connection, the callback is fired, but if there is no work
to be done, Node.js will sleep